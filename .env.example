# Hostinger SMTP 配置(用于联系表单发邮件)
# 复制本文件为 .env.local 并填入真实密码 — .env.local 不会被 git 上传
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=henry@resin-factory.com
SMTP_PASSWORD=your_email_password_here

# 询盘最终接收的邮箱(默认与 SMTP_USER 相同)
INQUIRY_TO_EMAIL=henry@resin-factory.com
